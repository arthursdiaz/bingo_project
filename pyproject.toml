[project]
name = "bingo"
version = "0.1.0"
description = "Personal desktop assistant for Linux"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "faster-whisper>=1.2.1",
    "rapidfuzz>=3.14.5",
    "websockets>=16.1",
]

[tool.uv]
package = true
