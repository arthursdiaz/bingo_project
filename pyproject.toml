[project]
name = "bingo"
version = "0.1.0"
description = "Personal desktop assistant for Linux"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "websockets>=16.1",
]

[tool.uv]
package = true